The current package provides the end users with the ability to build tanks from a json description.
It also allows integration with fighting strategy modules and computes the tank attributes (attack, defense, [tbd ..] ).